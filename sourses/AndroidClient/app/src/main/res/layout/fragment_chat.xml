<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools" android:layout_width="match_parent"
    android:layout_height="match_parent" tools:context="com.fupm.skeb.androidclient.ChatFragment">
    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:id="@+id/chat_relative_layout">


        <Button
            android:layout_width="100dp"
            android:layout_height="50dp"
            android:text="@string/send"
            android:id="@+id/buttonSend"
            android:layout_alignParentBottom="true"
            android:layout_alignParentRight="true"
            android:layout_alignParentEnd="true" />

        <EditText
            android:layout_width="260dp"
            android:layout_height="50dp"
            android:inputType="textAutoComplete"
            android:ems="4"
            android:id="@+id/message"
            android:layout_alignParentLeft="true"
            android:layout_alignParentStart="true"
            android:layout_alignParentBottom="true"
            android:textColor="#FAEEDD" />
        <ScrollView
            android:layout_width="150sp"
            android:layout_height="400sp"
            android:id="@+id/scrollView"
            android:layout_above="@+id/buttonSend"
            android:layout_alignRight="@+id/buttonSend"
            android:layout_alignEnd="@+id/buttonSend"
            android:layout_alignParentLeft="true"
            android:layout_alignParentStart="true"
            android:layout_alignParentTop="true"
            android:layout_margin="30dp">

            <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:id="@+id/chatLog"
                android:textStyle="bold|italic"
                android:textColor="#FAEEDD"
                android:textColorLink="#FAEEDD" />
        </ScrollView>

    </RelativeLayout>

</FrameLayout>
